
import React from "react";
import ReactDOM from "react-dom";

function App() {
   {console.info('hello')}
   return(
      <div>
         <h2>LicensePro React App</h2>
      </div>
   )
}

ReactDOM.render(<App />, document.getElementById("license-pro-root"));
